default: &default
  store_options:
    # Cap age of oldest cache entry to fulfill retention policies  
    max_size: 268435456  # 256 MB
    namespace: "powernode_cache"

development:
  <<: *default

test:
  <<: *default

production:
  database: cache
  <<: *default
