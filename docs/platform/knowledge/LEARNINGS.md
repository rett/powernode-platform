# Learnings & Patterns

> Auto-generated by `rails mcp:sync_docs` on 2026-03-08 05:30 UTC
> **Do not edit manually** — changes will be overwritten on next sync.
> Source: `ai_compound_learnings` | Filter: status IN (active, verified), importance >= 0.5 OR confidence >= 0.7, non-blank title, deduplicated

---

**100 entries** exported (max 100)

## Pattern (42)

### Knowledge maintenance jobs run in dependency cascade order — new jobs must slot before doc sync at 5:30 AM [VERIFIED]

Daily knowledge maintenance jobs run in dependency order: compound learning decay (3:45) → memory consolidation (4:00) → skill lifecycle (4:15) → shared knowledge import+quality recalc (4:30) → knowledge graph confidence decay+quality recalc (4:45) → doc sync (5:30). Each stage feeds the next: learnings decay first, then survivors import to shared knowledge, then graph nodes recompute, then everything syncs to docs. New maintenance jobs should be scheduled AFTER their data dependencies and BEFOR...

- **Importance**: 1.00 | **Confidence**: 0.95 | **Effectiveness**: -
- **Scope**: global | **Access count**: 0 | **Injections**: 1
- **Tags**: sidekiq, scheduling, maintenance, knowledge-lifecycle

### BaseTool#execute uses keyword params: argument — not positional [VERIFIED]

All AI tools inheriting from BaseTool use execute(params: {}) signature. Calling execute(action: x, conversation_id: y) raises ArgumentError: missing keyword: :params. Correct form: tool.execute(params: { action: "send_message", conversation_id: "...", message: "..." }). The MCP endpoint handles this wrapping automatically but direct rails runner invocations must pass the params hash explicitly. File: server/app/services/ai/tools/base_tool.rb:54.

- **Importance**: 0.70 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 1
- **Tags**: none

### ConciergeTool MCP integration pattern [VERIFIED]

ConciergeTool (Ai::Tools::ConciergeTool) wires ConciergeService into the MCP layer with 4 actions: send_concierge_message, confirm_concierge_action, list_conversations, get_conversation_messages. Key patterns: (1) find_or_create_concierge_conversation reuses active conversations via agent.conversations.active.where(user_id:) before creating new ones — mirrors conversations_controller#create_concierge. (2) send_concierge_message adds user message then calls ConciergeService#process_message which...

- **Importance**: 0.68 | **Confidence**: 0.62 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### LLM Proxy Architecture: Worker-to-Server Call Chain

## Pattern

All worker LLM calls are routed through the server's internal proxy — the worker never contacts LLM providers directly.

### Call Chain

1. **Worker job** includes `AiLlmProxyConcern` (worker/app/jobs/concerns/ai_llm_proxy_concern.rb)
2. Concern memoizes `LlmProxyClient` (worker/app/services/llm_proxy_client.rb), initialized with `backend_api_post` method reference
3. `LlmProxyClient` exposes: `complete`, `complete_with_tools`, `complete_structured`, `tool_definitions`, `dispatch_too...

- **Importance**: 0.66 | **Confidence**: 0.52 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 0 | **Injections**: 3
- **Tags**: none

### Knowledge quality requires active maintenance: decay, recalc, verification [VERIFIED]

Shared knowledge entries become stale without periodic quality_score recalculation (>24h threshold). Knowledge graph nodes need confidence decay applied to reduce certainty on unseen entities. Compound learnings need verification or dispute workflows to separate signal from noise. Without active quality maintenance, knowledge stores accumulate low-quality entries that degrade retrieval relevance. Run recalculate_all_quality on SharedKnowledge, decay_confidence! on stale KnowledgeGraphNodes, and...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Gitea multi-file feature workflow via API [VERIFIED]

When implementing a multi-file feature in a Gitea repository via API: 1) GET repo info to find default branch, 2) POST to create feature branch from default, 3) For each file: check if it exists first (GET contents), then POST (create) or PUT (update with SHA) accordingly, 4) POST to create pull request from feature branch to default. Key gotchas: Gitea contents API needs base64-encoded content, existing files require the SHA from GET for updates, and slashed branch names need resolve_ref to com...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### ActivityMonitorTool MCP integration pattern [VERIFIED]

ActivityMonitorTool (Ai::Tools::ActivityMonitorTool) provides 5 platform monitoring actions: get_activity_feed, get_mission_status, get_notifications, dismiss_notification, get_system_health. Key patterns: (1) get_activity_feed aggregates from 4 sources — ai_missions, ai_conversations, ai_execution_events, ai_execution_events.with_errors — using a configurable hours window. (2) get_mission_status has dual mode: no ID returns in_progress overview with awaiting_approval subset, with ID returns mis...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Feature gating three-layer system: ExtensionRegistry → FeatureGateService → Flipper [VERIFIED]

Powernode implements feature gating at three layers: (1) ExtensionRegistry.loaded?(:enterprise) — binary check whether a Rails Engine extension is registered. Used for entire feature modules. (2) Shared::FeatureGateService — orchestrates permission + extension + flag checks. Methods: enterprise_loaded?, feature_enabled?(flag), gate_check(feature, user). (3) Flipper feature flags — granular per-feature toggles (e.g., :compound_learning_injection, :compound_learning_promotion). Frontend equivalent...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### AI subsystem scope: agents, teams, workflows, memory, trust, protocols [VERIFIED]

The Powernode AI subsystem is the platform's largest module. Models (server/app/models/ai/): ~127 models covering agents (AiAgent, AgentLineage, AgentTrustScore, AgentBudget), teams (AgentTeam, AgentTeamMembership with role profiles), workflows (AiWorkflow, AiWorkflowNode, AiWorkflowEdge), memory tiers (AgentShortTermMemory, SharedKnowledge, MemoryPool), knowledge (CompoundLearning, KnowledgeGraphNode, KnowledgeGraphEdge), conversations (AiConversation, AiMessage, ChatSession), missions (AiMissi...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### DevOps subsystem: pipelines, git providers, containers, Docker Swarm [VERIFIED]

The Powernode DevOps subsystem (server/app/models/devops/, server/app/services/devops/) manages CI/CD and infrastructure. ~41 models including: Devops::Pipeline, Devops::PipelineStep, Devops::PipelineRun, Devops::GitProvider (GitHub + Gitea), Devops::GitRunner, Devops::ContainerRegistry, Devops::DockerSwarmCluster, Devops::DockerSwarmService. Key services: StepHandlerRegistry — registry pattern that maps pipeline step types to handler classes (build, test, deploy, review, scan). RunnerDispatchSe...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Powernode platform architecture overview: Rails 8 + React TS + Sidekiq [VERIFIED]

Powernode is a subscription lifecycle management platform with three main components: (1) Backend: Rails 8 API-only (server/) with JWT auth, UUIDv7 primary keys, PostgreSQL with pgvector, 120+ controllers under Api::V1 namespace, 460+ services, 352 database tables. (2) Frontend: React TypeScript (frontend/) with Tailwind CSS, theme-aware styling (bg-theme-*, text-theme-*), 11 feature modules (ai, dashboard, account, settings, devops, auth, support, docs, content, notifications, profile). (3) Wor...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Extension architecture: Rails Engine + ExtensionRegistry pattern [VERIFIED]

Powernode uses a modular extension architecture based on Rails Engines and a central registry. Backend: server/lib/powernode/extension_registry.rb maintains a registry of loaded extensions. Each extension is a Rails Engine (e.g., extensions/enterprise/lib/enterprise/engine.rb) with an extension.json manifest declaring routes, models, and feature flags. Extensions register via ExtensionRegistry.register(:enterprise) at engine initialization. Extensions use the decorator pattern to add association...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### WebSocket architecture: 17 ActionCable channels with WebSocketManager [VERIFIED]

Powernode uses ActionCable for real-time communication with 17 channels in server/app/channels/: AiConversationChannel (chat messages, streaming, AI responses), NotificationChannel (global notifications), PresenceChannel (user online status), AgentStatusChannel (agent health/trust changes), PipelineChannel (CI/CD run progress), MissionChannel (mission state transitions), WorkspaceChannel (workspace collaboration, @mentions), DashboardChannel (live metrics), plus domain-specific channels for bill...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Knowledge system internals: compound learnings, shared knowledge, graph, skills [VERIFIED]

Powernode's knowledge system has four interconnected subsystems: (1) CompoundLearning — extracted insights with pgvector embeddings, importance/confidence/effectiveness scores, status lifecycle (active→verified/disproven→superseded→archived). AutoExtractorService extracts from executions. Semantic search via nearest_neighbors with status: [active, verified]. Daily decay at 3:45 AM. (2) SharedKnowledge — curated reference entries with quality_score (composite 0-1 scale), ratings (1-5 feed into qu...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### JWT-Only Auth Consolidation: Worker ↔ Server

## Pattern: JWT-Only Bidirectional Authentication

The platform uses JWT-only authentication for all worker↔server communication. Raw `swt_` static tokens have been fully removed.

### Worker → Server (outgoing)
- `WorkerJwt.token` mints short-lived JWTs (5min expiry, 4min cache per-thread)
- Uses `WORKER_ID` + `JWT_SECRET_KEY` from environment
- Payload: `{type: "worker", sub: WORKER_ID, iss: JWT_ISSUER, aud: JWT_AUDIENCE}`
- Required in `worker/config/boot.rb` before any service that uses it
-...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### ChatWindowHeader must call closeTab after conversation deletion to clear stale view

When deleting a conversation via ChatWindowHeader handleDelete, the API call succeeded but the tab remained in ChatWindow state with no backing conversation — showing a blank/stale view. Fix: After successful conversationsApi.deleteConversation(), call closeTab(activeTab.id) from useChatWindow() context. The CLOSE_TAB reducer removes the tab from state.tabs and shifts activeTabId to the next available tab, or shows NewConversationTab if none remain. Same pattern applies to any destructive action...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### Execution Gate 5-Check Pipeline: Fail-Closed Governance

## Pattern

`Ai::Autonomy::ExecutionGateService` runs a 5-check pre-execution governance pipeline. It is FAIL-CLOSED: any check returning a blocking result halts execution immediately.

### Check Order (sequential, short-circuits on first block)

1. **check_capability** — `CapabilityMatrixService#check(agent:, action_type:)` evaluates whether the agent's trust tier permits the requested action. Returns `:denied` or `:requires_approval` if blocked.
2. **check_budget** — Queries `Ai::AgentBudget.a...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### Trust Tier Capability Matrix: 4-Tier x 10-Action Permission System

## Pattern

CapabilityMatrixService implements a declarative trust-tier capability matrix governing agent actions based on trust level.

### Trust Tiers
1. supervised - Default. Only read_data allowed. Everything else denied or requires approval.
2. monitored - read_data + execute_tool allowed. Most others require approval.
3. trusted - Most actions freely. modify_system, delete_data, modify_permissions require approval.
4. autonomous - Full access except delete_data and modify_permissions (alwa...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### Team Strategy Dispatch: StrategyFactory and 4 Execution Modes

## Pattern

Ai::TeamStrategies::StrategyFactory dispatches team executions to one of 4 strategy implementations based on team_type.

### Strategy Map
- sequential -> SequentialStrategy (also used for workspace type)
- parallel -> ParallelStrategy
- hierarchical -> HierarchicalStrategy
- mesh -> MeshStrategy
- workspace -> SequentialStrategy (alias)

### Architecture
- All strategies inherit from BaseStrategy
- BaseStrategy requires: team, execution, account (constructor args)
- All implement exe...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### AI Cost Calculation 3-Tier Resolution Chain

AiCostCalculationConcern (worker/app/jobs/concerns/ai_cost_calculation_concern.rb) resolves pricing through a 3-tier chain: Tier 1 - Model pricing table: fetch_model_pricing calls GET /api/v1/ai/autonomy/pricing/lookup with provider_type and model_id. Returns input_per_1k and output_per_1k from ai_model_pricings table. Tier 2 - Provider/credential config: Falls back to provider.configuration.pricing or credentials.pricing hash with prompt_cost_per_1k and completion_cost_per_1k keys. Tier 3 - Har...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### Agent Router 5-Dimension Scoring Model

Ai::Routing::AgentRouterService scores agents across 5 weighted dimensions to route tasks. Weights: capability(0.30), trust(0.25), skill_match(0.25), performance(0.10), cost_efficiency(0.10). Capability: Uses CapabilityMatrixService.check - allowed=1.0, requires_approval=0.5, denied=0.0. Trust: From AgentTrustScore.overall_score, default 0.3 if no score exists. Skill match: Keyword overlap between task words (4+ chars) and agent name/description/system_prompt/capabilities. Performance: Ratio of...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### MCP Session Lifecycle Pattern

McpSession manages MCP protocol sessions with these lifecycle rules: 24h TTL (DEFAULT_TTL). Sliding renewal via touch_activity! extends expires_at when within 1h of expiry using update_columns. Statuses: active/expired/revoked. Active scope = status active AND expires_at > now. Agent linkage via link_agent! sets ai_agent_id. On deactivation calls McpClientIdentityService.deactivate_agent. Stale detection scope finds sessions with last_activity_at older than 1h. Account-scoped with for_account sc...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### Worker BaseJob Infrastructure Pattern

All Powernode worker jobs inherit from BaseJob (worker/app/jobs/base_job.rb). Key infrastructure:

1. **execute() not perform()**: BaseJob wraps perform() — subclasses implement execute(*args). perform() handles timing, logging, success/failure recording.

2. **Runaway loop detection**: check_runaway_loop runs before every execute. Thresholds: >=5 executions in 60s triggers disable (5min cooldown via Redis key job_disabled:). >=15 executions in 300s triggers 5s sleep delay. Uses job_key = ClassN...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### Conformance Engine Temporal Rules - Execution Gate Pipeline

## Conformance Engine Overview

File: `server/app/services/ai/autonomy/conformance_engine_service.rb`

The ConformanceEngineService enforces temporal ordering constraints on AI agent actions. It verifies that required prerequisite events occurred within a time window before allowing a trigger event.

## Default Temporal Rules

| Rule Name | Trigger Event | Required Prior Event | Window | Severity |
|-----------|--------------|---------------------|--------|----------|
| approval_before_execution...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### Enterprise Feature Gate Pattern - Three-Layer Gating Architecture

## Three-Layer Gating Architecture

Powernode uses a three-layer feature gate to handle the enterprise/core mode split:

### Layer 1: Backend — FeatureGateService

File: `server/app/services/shared/feature_gate_service.rb`

**Key methods:**
- `FeatureGateService.enterprise_loaded?` — checks if enterprise engine is registered
- `FeatureGateService.enterprise_enabled?` — loaded AND Flipper flag `:enterprise_mode` is on
- `FeatureGateService.available?(feature, account:, user:)` — core features alw...

- **Importance**: 0.54 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 1
- **Tags**: none

### CPCV: Combinatorial Purged Cross-Validation for Backtesting

## Pattern: Combinatorial Purged Cross-Validation (CPCV)

Standard k-fold CV leaks information in time-series backtests because training data contains future information relative to test folds. CPCV (Lopez de Prado, 2018) fixes this with two mechanisms:

1. **Purging**: Remove training samples within a purge window of any test sample to eliminate look-ahead bias. Set purge window = 2x `tick_interval_seconds` from `Trading::Strategy` to account for signal generation lag.
2. **Embargo**: After pur...

- **Importance**: 0.53 | **Confidence**: 0.52 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Regime-Switching Factor Investing for Dynamic Capital Allocation

## Pattern: Regime-Switching Factor Allocation

Extends HMM regime detection (learning #6) to dynamically reallocate capital across strategy types based on current market regime. Different factors (momentum, value/mean-reversion, volatility) perform differently across regimes.

### Integration with Powernode Trading

- Operates on `Trading::Portfolio.strategies.active` collection
- Modifies `allocated_capital_usd` per strategy based on regime-factor affinity scores
- Regime detection feeds into...

- **Importance**: 0.53 | **Confidence**: 0.52 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### HMM Market Regime Detection for Strategy Routing

## Pattern: Hidden Markov Model (HMM) Regime Detection

Markets alternate between distinct regimes (bull/bear/sideways/high-vol). An HMM can detect these regime transitions from observable market features and route strategies accordingly.

### State Mapping to Existing Infrastructure
HMM states map directly to `Trading::Simulation::MarketDataGenerator::SCENARIOS`:
- State 0 (Low-vol trending) -> 'bull_run' scenario
- State 1 (High-vol trending) -> 'bear_crash' scenario
- State 2 (Low-vol mean-re...

- **Importance**: 0.53 | **Confidence**: 0.52 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### AutoML Hyperparameter Tuning via Ralph Loop Integration

## Pattern: AutoML for Strategy Parameter Optimization

AutoML frameworks (Optuna, HyperOpt patterns) automate hyperparameter search across strategy parameters. This maps naturally to Powernode's existing Ralph Loop infrastructure.

### Integration with Ralph Loop

- `Trading::Strategy.ralph_loop` (belongs_to `Ai::RalphLoop`) provides the optimization loop
- Ralph Loop `loop_type: 'parameter_optimization'` drives the search
- Search space: all numeric keys in `Strategy.parameters` JSON (e.g., po...

- **Importance**: 0.53 | **Confidence**: 0.52 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### DSR: Deflated Sharpe Ratio for Multiple Testing Correction

## Pattern: Deflated Sharpe Ratio (DSR)

When testing N strategy variants, the best Sharpe ratio is biased upward. DSR (Bailey & Lopez de Prado, 2014) corrects for this by computing the probability that the observed Sharpe exceeds the expected maximum Sharpe under the null hypothesis of zero skill.

### Formula
DSR = P(SR_observed > SR_max_expected)
where SR_max_expected = sqrt(V[SR]) * ((1 - gamma) * Phi_inv(1 - 1/N) + gamma * Phi_inv(1 - 1/(N*e)))
V[SR] accounts for skewness and kurtosis of re...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Streamable HTTP controller extracts SSE callbacks into dedicated builder methods

The MCP streamable HTTP controller's SSE stream action was refactored to extract the monolithic inline callback into two dedicated methods:

1. build_session_callback(sse) — handles MCP JSON-RPC 2.0 notifications (spec-compliant event: message)
2. build_workspace_callback(channel, sse, agent, dedup) — handles workspace events with:
   - ALLOWED_WORKSPACE_EVENTS filtering
   - @mention detection (structural metadata + text @Name patterns)
   - Session-level deduplication (event_type:msg_id key)...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### White's Reality Check: Bootstrap Test for Strategy Significance

## Pattern: White's Reality Check (Bootstrap)

White's Reality Check (2000) tests whether the best strategy's performance is significantly better than a benchmark (e.g., buy-and-hold) after accounting for data snooping across multiple strategy comparisons.

### Algorithm
1. Compute performance differential d_t = strategy_return_t - benchmark_return_t for each period t
2. Generate B=1000 bootstrap samples using block bootstrap (preserving autocorrelation)
3. For each bootstrap sample, compute the...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Monte Carlo Permutation and Bootstrap for Strategy Confidence Intervals

## Pattern: Monte Carlo Bootstrap for Strategy Performance Confidence

Monte Carlo bootstrap generates confidence intervals for strategy performance metrics by resampling returns, quantifying uncertainty around point estimates like Sharpe ratio and max drawdown.

### Already Supported Infrastructure
- `SimulationMode 'monte_carlo'` enum value exists on `Trading::Simulation`
- `MarketDataGenerator.generate_candles` with configurable volatility/trend provides price scenarios
- `PerformanceCalculat...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Correlation Breakdown Scenario Testing for Portfolio Stress

## Pattern: Correlation Breakdown Scenarios

During market crises, asset correlations spike toward 1.0 (everything falls together). Testing strategies under correlation breakdown reveals hidden portfolio concentration risk.

### Integration with Powernode Trading

- Add `correlation_override` parameter to `Trading::Simulation::MarketDataGenerator`
- When specified, override the historical correlation matrix with a stress matrix
- Stress scenarios: all pairs correlation -> 0.9 (crisis), or correl...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Almgren-Chriss Market Impact Model for Large Order Execution

## Pattern: Almgren-Chriss Market Impact Model

For large orders, market impact has two components: temporary (price recovers after trade) and permanent (price shift that persists). The Almgren-Chriss model quantifies both.

### Model
- Temporary impact: `h(v) = eta * sign(v) * |v/V|^beta` where v=trade rate, V=market volume, beta~0.6
- Permanent impact: `g(v) = gamma * v/V` (linear in trade rate)
- Total cost = temporary + permanent + spread

### Integration with Powernode Trading

- Extends `T...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Parameter Sensitivity Analysis and Robustness Surface Mapping

## Pattern: Sensitivity Analysis for Strategy Parameter Robustness

A robust strategy should perform well not just at optimal parameters, but across a neighborhood of parameter values. Sensitivity analysis maps the fitness surface to identify fragile vs robust optima.

### Algorithm
1. Start from optimized parameters in `Strategy.parameters`
2. For each parameter p:
   a. Vary p across range [p * 0.5, p * 1.5] in 10 steps, holding others constant
   b. Run backtest for each value via `Simulation...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Vectorized Backtesting for Fast Evolution Screening

## Pattern: Vectorized (Matrix-Based) Backtesting

Vectorized backtesting evaluates strategies by applying signal vectors to return matrices using linear algebra, without simulating individual ticks. This is 10-100x faster than event-driven backtesting.

### Approach
1. Precompute signal matrix S[strategy, time] from indicators
2. Precompute return matrix R[pair, time] from `PriceSnapshot` history
3. PnL = sum(S * R) per strategy (element-wise multiply, then sum)
4. Sharpe = mean(S * R) / std(S...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Event-Driven Backtesting: Current Simulation Architecture Pattern

## Pattern: Event-Driven Backtesting Architecture (Current Implementation)

Powernode's current backtest architecture is event-driven: each tick processes market events sequentially through the full trading pipeline including risk checks, signal generation, order execution, and position management.

### Architecture in Powernode Trading

The event loop lives in `Trading::Simulation::Controller.run_cycle!`:
1. `advance_prices!` — generate new market tick via `VenueAdapter::Simulator`
2. `portfoli...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Trading signal quality optimizations: exit gating, weighted consensus, logit transforms, time decay

Key signal improvements across 9 strategies: (1) LLM Probability: cooldown no longer blocks exit signals, exit edge threshold raised from 0.2x to 0.5x entry threshold, max_spread_pct gate added. (2) Agent Ensemble: added bear rebuttal round for balanced debate, role-weighted consensus (fundamentals 1.5x, risk_manager 1.3x, technical 1.2x), separate debate_temperature param. (3) Combinatorial Arbitrage: scan_interval reduced 600s→180s, similarity_threshold 0.7→0.55 to catch more arb opportunities...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 236 | **Injections**: 0
- **Tags**: none

### Configurable initial_balance for trading training sessions

Pattern: Use existing config JSONB column to store initial_balance without migration. Extract from params directly (not session_params) to avoid ActiveModel::UnknownAttributeError. Thread through: controller -> session.config -> internal controller -> LiveTrainingRunner.run!(initial_balance:) -> setup_portfolio! + create_strategies! + generate_report. Per-strategy capital = initial_balance / (pairs * strategy_types). Default 100k for backward compat. Files: live_training_runner.rb, training_sess...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### ConciergeToolBridge filters to ~25 workspace tools to improve LLM tool selection

In workspace conversations, ConciergeToolBridge now restricts available tools from ~84 to ~25 via WORKSPACE_TOOLS whitelist. This makes send_message immediately visible to the LLM instead of buried in a massive tool list.

WORKSPACE_TOOLS whitelist includes: send_message, invite_agent, list_messages, list_workspaces, active_sessions, create_workspace, list_agents, get_agent, execute_agent, list_teams, get_team, execute_team, add_team_member, search_knowledge, query_learnings, search_knowledge_gr...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Mention segment parsing routes per-agent instructions from multi-mention messages

Ai::Conversation#parse_mention_segments(content) splits messages with multiple @mentions into a preamble and per-agent segments. This allows each agent to receive only its targeted instruction rather than the full message.

Algorithm:
1. Get workspace team members, sorted by name length (longest first to prevent partial matches)
2. Find all @mention positions (overlap-aware scanning)
3. Extract preamble (text before first mention)
4. Extract segments: text between each mention and the next menti...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

## Best Practice (12)

### Every _id foreign key column MUST have a corresponding belongs_to association — programmatic writes are not exempt [VERIFIED]

CompoundLearning had verified_by_id and disproven_by_id columns written by verify!/disprove! methods, but no belongs_to associations. This meant the foreign keys could not be hydrated via ActiveRecord (learning.verified_by returned nil even with a valid ID). Rule: every _id column that stores a foreign key MUST have a corresponding belongs_to association, even if the column is only written programmatically. Use reflect_on_association in rails console to audit.

- **Importance**: 0.85 | **Confidence**: 0.95 | **Effectiveness**: -
- **Scope**: global | **Access count**: 0 | **Injections**: 0
- **Tags**: activerecord, associations, foreign-keys, compound-learning

### Gitea Actions workflows belong in .gitea/workflows/

Gitea Actions CI/CD workflows must be placed in .gitea/workflows/ directory (not .github/workflows/). Gitea Actions are compatible with GitHub Actions syntax but use Gitea-specific paths. Workflow files use YAML format and support: push/pull_request triggers, schedule (cron), workflow_dispatch (manual), and can use GitHub Actions marketplace actions like actions/checkout@v4 and actions/upload-artifact@v4.

- **Importance**: 0.73 | **Confidence**: 0.50 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 0 | **Injections**: 3
- **Tags**: none

### MCP tool action_definitions pattern for per-action schemas [VERIFIED]

Multi-action tool classes should implement action_definitions class method returning a hash of {tool_name => {description:, parameters:}} for each action. BaseTool provides a default that strips :action from single-action tools. The PlatformApiToolRegistry uses action_definitions instead of raw definition to expose focused per-action schemas to MCP clients. This avoids exposing internal action routing parameters and produces cleaner JSON Schema for each tool.

- **Importance**: 0.70 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 1 | **Injections**: 0
- **Tags**: none

### E2E MCP smoke test pattern for platform verification [VERIFIED]

An E2E smoke test pattern for verifying MCP platform tool coverage: Phase 0 (Pre-flight): Run all diagnostic tools (knowledge_health, skill_health, memory_stats, graph_statistics, learning_metrics, skill_metrics) and store baselines in shared_memory. Phase 1 (Planning): Use discovery tools (search_knowledge, query_learnings, search_knowledge_graph, reason_knowledge_graph, discover_skills, get_api_reference, search_documents, query_knowledge_base) then create knowledge and extract to KG. Phase 2...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Permission system: three-tier resource/admin/system categories [VERIFIED]

Powernode's permission system is defined in server/config/permissions.rb with three tiers: (1) Resource permissions — CRUD on domain objects (users.read, users.manage, subscriptions.manage, ai.agents.read, ai.agents.manage, devops.pipelines.manage). (2) Admin permissions — account-level administration (account.manage, roles.manage, billing.manage). (3) System permissions — platform-wide operations (system.admin, system.impersonate). Permission checks: Backend uses current_user.has_permission?('n...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### MCP tool development pattern: BaseTool → action_definitions → PlatformApiToolRegistry [VERIFIED]

MCP tool development in Powernode follows a consistent pattern across 25 tool classes in server/app/services/ai/tools/. (1) Inherit from BaseTool which provides execute(params:) signature, account/agent context, error handling. (2) Define action_definitions class method returning a hash of action→{description, parameters} for MCP schema exposure. Multi-action tools map one Ruby class to many MCP tools. (3) Register in PlatformApiToolRegistry::TOOLS hash mapping action strings to tool classes. (4...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Team Channel Bridge Anti-Echo Pattern

The Ai::TeamChannelBridgeService provides bidirectional sync between ActionCable Chat::Channels and Ai::TeamChannels. Anti-echo protection prevents infinite loops:

1. **Inbound (Chat→Team)**: sync_inbound_to_team_channel creates TeamMessage with metadata including source=chat_bridge and chat_channel_id. This metadata is the echo-prevention key.

2. **Outbound (Team→Chat)**: sync_outbound_to_platform checks each bridged chat channel. It skips the originating channel by comparing source=chat_brid...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### Portfolio-Level Multi-Strategy Backtesting

## Best Practice: Portfolio-Level Testing for Multi-Strategy Portfolios

Individual strategy backtests miss critical portfolio-level effects: capital competition, correlation between strategies, combined drawdown dynamics, and rebalancing costs.

### Already Supported Infrastructure
- `Trading::Portfolio.strategies.active` — portfolio owns multiple strategies
- `Trading::Simulation::Controller.run_cycle!` already iterates all active strategies per tick
- `PerformanceCalculatorService` calculates...

- **Importance**: 0.56 | **Confidence**: 0.54 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Reverse Stress Testing: Finding Strategy Breaking Points

## Best Practice: Reverse Stress Testing

Instead of asking 'how does the strategy perform under scenario X?', reverse stress testing asks 'what scenario would cause the strategy to breach risk limits?' This is the inverse of standard stress testing.

### Algorithm
1. Define failure criterion: breach of `RiskProfile.max_portfolio_drawdown_pct`
2. Start from current market conditions in `Trading::VenueAdapter::Simulator`
3. Use adversarial optimization to find the minimal market perturbation that...

- **Importance**: 0.53 | **Confidence**: 0.52 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Walk-Forward Optimization (WFO) Implementation in Trading Simulation

## Best Practice: Walk-Forward Optimization (WFO)

WFO is the gold standard for strategy parameter optimization. It divides historical data into sequential windows, optimizing parameters on each in-sample window and testing on the subsequent out-of-sample window, then advancing forward.

### Already Supported Infrastructure
- `SimulationMode 'walk_forward'` enum value exists on `Trading::Simulation`
- `Trading::StrategyVersion` tracks parameter changes with `version_number`, `parameters`, `chang...

- **Importance**: 0.53 | **Confidence**: 0.52 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Bonferroni Correction: Simple Multiple Testing Adjustment for Evolution

## Best Practice: Bonferroni Correction for Evolution Candidate Evaluation

The simplest multiple testing correction: divide significance threshold by the number of tests. When evaluating N strategy candidates in evolution, the chance of finding a 'significant' result by luck alone increases with N.

### Formula
adjusted_alpha = base_alpha / N
where N = number of candidates tested

### Integration with Powernode Trading

- N = `Trading::EvolutionCandidate.where(trading_evolution_epoch_id: epoch....

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Hybrid Backtesting: Vectorized Screen plus Event-Driven Validate

## Best Practice: Hybrid Backtesting Architecture

Combine vectorized backtesting (learning #28) for fast initial screening with event-driven backtesting (learning #29) for detailed validation. This gives 10x speedup for evolution while maintaining full execution realism.

### Two-Phase Pipeline

**Phase 1: Vectorized Screening (fast)**
- Compute signal and return matrices for all evolution candidates
- Calculate approximate fitness (Sharpe, returns) without execution modeling
- Select top N% ca...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

## Discovery (18)

### Dead maintenance methods pattern: wire model lifecycle methods to jobs and endpoints in the same PR [VERIFIED]

KnowledgeGraphNode#decay_confidence! and #recalculate_quality_score! existed since initial implementation but were never called by any job or endpoint. SharedKnowledgeService#recalculate_all_quality was similarly orphaned. Pattern: when adding model methods for lifecycle management, always wire them into a scheduled job AND an API endpoint in the same PR. Check for orphaned methods by grepping for the method name across jobs/ and controllers/.

- **Importance**: 1.00 | **Confidence**: 0.90 | **Effectiveness**: -
- **Scope**: global | **Access count**: 0 | **Injections**: 1
- **Tags**: maintenance, knowledge-graph, shared-knowledge, orphaned-code

### OAuth scope drift between Doorkeeper config and well-known endpoint breaks MCP client discovery [VERIFIED]

WellKnownController#oauth_scopes returned 5 hardcoded scopes while Doorkeeper.configure defined 8 via optional_scopes. The well-known endpoints are the OAuth discovery surface for MCP clients — scope drift means clients cannot request billing/users/webhooks scopes they are entitled to. Fix: derive scopes from Doorkeeper config or keep a single source of truth. Also found dead scope_descriptions local variable in doorkeeper.rb initializer — Doorkeeper has no built-in scope_descriptions config met...

- **Importance**: 0.90 | **Confidence**: 0.90 | **Effectiveness**: -
- **Scope**: global | **Access count**: 0 | **Injections**: 0
- **Tags**: oauth, doorkeeper, mcp, well-known, scope-drift

### MCP inputSchema must include required:[] even when empty [VERIFIED]

JSON Schema spec and strict MCP clients expect the required field to always be present on object-type schemas, even as an empty array. The build_input_schema method in StreamableHttpController must return {type: object, properties: {}, required: []} for parameterless tools. Omitting required causes validation failures in Claude Desktop, Cursor, and other MCP consumers. 10 platform tools were affected: list_agents, list_teams, list_workflows, list_pools, list_knowledge_bases, learning_metrics, sk...

- **Importance**: 0.70 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 2 | **Injections**: 0
- **Tags**: none

### SharedKnowledgeService SIMILARITY_THRESHOLD was 0.85, too strict for search [VERIFIED]

The SharedKnowledgeService SIMILARITY_THRESHOLD of 0.85 caused search to return 0 results because OpenAI text-embedding-3-small produces cosine similarity scores of 0.3-0.7 for genuinely related content. Threshold lowered to 0.5 (distance <= 0.5). The DEDUP_THRESHOLD of 0.92 remains correct for duplicate detection. CompoundLearningService already used distance <= 0.6 (similarity >= 0.4) which was correct.

- **Importance**: 0.70 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 1
- **Tags**: none

### CompoundLearning.semantic_search excluded verified learnings via active scope [VERIFIED]

CompoundLearning.semantic_search used the active scope which only includes status=active records. Verified learnings (the most trusted) were invisible to context building. Fixed by replacing active scope with where(status: [active, verified]). This is critical because verified learnings should have higher priority in context injection, not be excluded entirely.

- **Importance**: 0.70 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 1
- **Tags**: none

### Extension extraction from core (Feb 2026): marketplace → enterprise, marketing → extension, supply-chain → submodule [VERIFIED]

In Feb 2026, Powernode underwent a major architecture change — three feature sets were extracted from core into modular extensions: (1) Marketplace features (billing, payments, reseller, BaaS) moved into extensions/enterprise/ as a git submodule (remote: git@git.ipnode.net:powernode/powernode-enterprise.git). Uses Rails Engine pattern with AccountDecorator adding billing associations. (2) Marketing features (campaigns, analytics, content) extracted to extensions/marketing/ as a standalone extens...

- **Importance**: 0.65 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Skill Usage Pipeline: Diagnosis of Zero-Usage Problem (4 Root Causes)

## Investigation Summary

42/43 skills show usage_count=0 despite a fully functional skill graph (43 nodes with embeddings), 47 agent-skill linkages, and a working ContextEnrichmentService that reads skills during execution.

## Root Cause 1: skill_self_learning Feature Flag is DISABLED

The `SelfLearningService.record_skill_outcomes()` method — the only automated mechanism to record skill usage from agent executions — is gated behind `Shared::FeatureFlagService.enabled?(:skill_self_learning, ac...

- **Importance**: 0.59 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 2
- **Tags**: none

### Boltzmann Machines for Synthetic Order Book Generation

## Discovery: Restricted Boltzmann Machines (RBMs) for Microstructure Simulation

RBMs can learn the joint distribution of order book states and generate realistic synthetic order books with proper bid-ask dynamics, queue positions, and market impact patterns.

### Integration with Powernode Trading

- Extends `Trading::VenueAdapter::Simulator` microstructure simulation
- Pairs with `MarketDataGenerator.generate_orderbook(pair:, spread_bps:, depth_levels:)` for more realistic order book dynamics...

- **Importance**: 0.56 | **Confidence**: 0.54 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Oversized File Registry - Controllers and Services Exceeding Limits

## Oversized Controllers (>300 lines, limit is 300)

| File | Lines | Over By |
|------|-------|---------|
| api/v1/admin/reverse_proxy_controller.rb | 910 | +610 |
| api/v1/analytics_controller.rb | 808 | +508 |
| api/v1/reports_controller.rb | 664 | +364 |
| api/v1/workers_controller.rb | 598 | +298 |
| api/v1/mcp/streamable_http_controller.rb | 587 | +287 |
| api/v1/ai/conversations_controller.rb | 560 | +260 |
| api/v1/admin/maintenance/maintenance_controller.rb | 547 | +247 |
| api/v1/webho...

- **Importance**: 0.54 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 1
- **Tags**: none

### Pages model requires account ownership and non-nil author

**Problem**: `Page` model had no `account_id` column and `belongs_to :user` was `optional: true`. Pages created by MCP tools had nil authors, causing `NoMethodError: undefined method 'id' for nil:NilClass` when serializing in the admin pages controller.

**Solution**:
- Added `account_id` column (UUID, NOT NULL, foreign key) via migration
- Changed `belongs_to :user` to required (removed `optional: true`)
- Migration backfilled all existing pages to admin account and admin user
- Scoped admin co...

- **Importance**: 0.54 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 1
- **Tags**: none

### Deep Reinforcement Learning Agents for Strategy Optimization

## Discovery: Deep RL Agents in Backtesting

Deep RL can learn trading policies directly from market interaction, treating the backtest environment as an RL problem. The agent learns optimal actions (entry/exit/position sizing) through trial-and-error in simulation.

### Mapping to Powernode Trading as RL Environment

- **State**: Market data from `VenueAdapter::Simulator.fetch_ticker` + position info from `Strategy.open_positions`
- **Action space**: Signal generation (buy/sell/hold + position...

- **Importance**: 0.53 | **Confidence**: 0.52 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Neural Feature Engineering and Sentiment Analysis for Signal Generation

## Discovery: ML-Generated Features for Strategy Signals

Neural networks can discover non-linear feature combinations from raw market data that traditional indicators miss. These learned features extend the `indicators` hash produced by strategy evaluation.

### Integration with Powernode Trading

- Extends `Trading::Strategies::Base.evaluate` method's signal generation
- Current flow: `StrategyEngine.evaluate(market_data)` -> strategy_class.new(strategy, market_data).evaluate -> returns signal...

- **Importance**: 0.53 | **Confidence**: 0.52 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### GANs for Synthetic Market Data Generation

## Discovery: Generative Adversarial Networks for Synthetic Price Data

GANs can generate synthetic market data that preserves statistical properties (fat tails, volatility clustering, mean reversion) of real markets, providing unlimited training data for backtests.

### Integration with Powernode Trading

- Replaces or augments `Trading::Simulation::MarketDataGenerator.generate_candles` for synthetic data mode
- Generator network learns from historical `PriceSnapshot` data distribution
- Discri...

- **Importance**: 0.53 | **Confidence**: 0.52 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Trading Training Session Performance Profile: AI strategies dominate classic, combinatorial arbitrage is top performer

## Training Session Results (2026-03-08)

15-tick session across 3 Polymarket markets with all 8 strategy types.

### Key Findings
- **Total P&L**: $1,418.81 (1.42% return on $100k)
- **Win rate**: 86.3% (82W / 13L)
- **LLM cost**: $9.60 (147.8x P&L/cost ratio)
- **AI vs Classic**: AI strategies +$1,598.59 vs Classic -$179.78

### Strategy Rankings
1. combinatorial_arbitrage: +$1,211.63 (85% of P&L)
2. sentiment_analysis: +$312.24 (best LLM ROI at 1,734x)
3. prediction_market: +$93.98 (no LLM co...

- **Importance**: 0.50 | **Confidence**: 1.00 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 348 | **Injections**: 12
- **Tags**: none

### Live smoke tests against efficient markets may produce zero signals — this is correct behavior

When running smoke tests against live prediction market data, highly efficient markets (prices near 0 or 1) may not produce any trading signals. This is expected: (1) Markets with prices outside 0.01-0.99 are skipped by PredictionMarket strategy. (2) Markets with prices near 0.99 have minimal trend momentum, so estimated probability barely differs from market price. (3) With a 3% edge threshold, efficient markets rarely trigger signals. (4) To force signal generation for pipeline validation, low...

- **Importance**: 0.50 | **Confidence**: 0.90 | **Effectiveness**: -
- **Scope**: team | **Access count**: 9168 | **Injections**: 1
- **Tags**: none

### Offline Reinforcement Learning on Historical Simulation Data

## Discovery: Offline RL from Completed Simulations

Offline RL (batch RL) learns policies from pre-collected data without requiring live environment interaction. This is ideal for trading because:
1. Live exploration is expensive/risky
2. We already have rich historical data from completed simulations

### Data Source

- `Trading::Simulation` model stores complete `timeline` JSON array with per-tick snapshots
- Each timeline entry contains: portfolio_pnl, portfolio_capital, active_strategies, o...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### gpt-4.1-mini ignores tool-use instructions — force tool_choice for delegation

gpt-4.1-mini frequently ignores system prompt instructions to call send_message for delegation. It responds with text like "I cannot communicate with Claude" instead of calling the tool. The fix is to detect delegation intent via regex and force tool_choice.

Detection: DELEGATION_PATTERN = /\b(ask|tell|have|message|ping|notify)\s+(claude|the\s+assistant)/i

When matched in a workspace conversation, ConciergeService sets:
  opts[:tool_choice] = { "type" => "function", "function" => { "name" => "...

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

### Bug fix: TradingLearningService injection outcome tracking used wrong model

update_injection_outcomes! passed compound learning IDs to ExperienceReplayService which looked up ExperienceReplay records instead of CompoundLearning records. find_by returned nil so injection_count and outcome counts were never updated. Fix: call CompoundLearning.find_by directly. File: extensions/trading/server/app/services/trading/trading_learning_service.rb

- **Importance**: 0.50 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: none

## Failure Mode (3)

### TypeScript any-Type Hotspots in Frontend Codebase

## Summary

**Total `any` type occurrences: 335** (including test files)
**Production code only: 104** (excluding .test. files)

## Worst Offenders (production code)

| File | Count | Context |
|------|-------|---------|
| shared/services/ai/AgentsApiService.ts | 8 | API service — likely response types not properly typed |
| shared/services/ai/MonitoringApiService.ts | 6 | API service — monitoring payloads untyped |
| features/admin/storage/services/storageApi.ts | 6 | Storage API responses unty...

- **Importance**: 0.54 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 1
- **Tags**: none

### Logging Violations Across Backend and Frontend

## Ruby Backend: puts/print Violations

**Total puts occurrences: 6** (should be 0, rule: use Rails.logger only)

Offending files:
- `server/app/services/storage_providers/smb_storage.rb` — 3 puts statements
- `server/app/services/ai/knowledge_population/populator_service.rb` — 2 puts statements
- `server/app/services/ai/knowledge_population/document_generator_service.rb` — 1 puts statement

No bare `print` statements found. No stray `pp` debug calls found.

## Frontend: console.log/debug/info/w...

- **Importance**: 0.54 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 1
- **Tags**: none

### Worker Boot Order: Manual require_relative Dependencies

## Failure Mode: Uninitialized Constant in Worker Services

The Powernode worker (`worker/`) is a standalone Sidekiq process, NOT a Rails app. It has no Zeitwerk autoloader. All classes must be explicitly loaded via `require_relative` in `worker/config/boot.rb`.

### Symptom
`NameError: uninitialized constant BackendApiClient::WorkerJwt` — Ruby looks for `WorkerJwt` in the lexical scope of `BackendApiClient` first, then top-level. But if the file defining `WorkerJwt` was never loaded, the consta...

- **Importance**: 0.54 | **Confidence**: 0.50 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 1
- **Tags**: none

## Reflexion (25)

### [REFLEXION] The arbitrage strategy entered a position at 0.386 and exited at 0.392, realizing a loss despite ...

Root cause: The arbitrage strategy entered a position at 0.386 and exited at 0.392, realizing a loss despite a positive price movement, indicating the strategy failed to account for transaction costs, slippage, or market impact that exceeded the 1.0% minimum spread threshold.
Error class: logic_error
Counterfactual: The agent should have validated that the net arbitrage spread (after accounting for fees, slippage estimates, and execution costs) exceeded the minimum threshold before entry, or dyn...

- **Importance**: 0.80 | **Confidence**: 1.00 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 8346 | **Injections**: 12
- **Tags**: reflexion, logic_error

### [REFLEXION] The arbitrage spread of approximately 0.13% (entry: 0.39251937 → exit: 0.392) fell below the conf...

Root cause: The arbitrage spread of approximately 0.13% (entry: 0.39251937 → exit: 0.392) fell below the configured minimum threshold of 1.0%, resulting in a loss-making trade execution despite LLM validation being enabled.
Error class: logic_error
Counterfactual: The agent should have rejected this trade opportunity at the pre-execution validation stage since the realized spread (0.13%) was substantially below min_arb_spread_pct (1.0%), or the confidence_threshold (0.3) was too permissive for t...

- **Importance**: 0.75 | **Confidence**: 1.00 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 3510 | **Injections**: 12
- **Tags**: reflexion, logic_error

### [REFLEXION] The confidence threshold of 0.15 was too permissive, allowing entry into a low-conviction trade t...

Root cause: The confidence threshold of 0.15 was too permissive, allowing entry into a low-conviction trade that moved against the position, resulting in a -0.02 loss on a minimal price movement (0.31900094 → 0.32).
Error class: configuration
Counterfactual: The agent should have either (1) increased the confidence_threshold to ≥0.50 to filter out marginal sentiment signals, (2) implemented a tighter stop-loss relative to position size, or (3) required multiple corroborating sentiment sources be...

- **Importance**: 0.75 | **Confidence**: 1.00 | **Effectiveness**: -
- **Scope**: team | **Access count**: 3636 | **Injections**: 1
- **Tags**: reflexion, configuration

### [REFLEXION] The arbitrage strategy entered a position at 0.6256 expecting convergence but the market moved ag...

Root cause: The arbitrage strategy entered a position at 0.6256 expecting convergence but the market moved against the position to 0.64, indicating the identified spread was either illusory, based on stale data, or the arbitrage legs failed to execute simultaneously, resulting in directional exposure rather than market-neutral profit.
Error class: logic_error
Counterfactual: Before executing the trade, the agent should have: (1) validated that both legs of the arbitrage could be simultaneously f...

- **Importance**: 0.75 | **Confidence**: 1.00 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 319 | **Injections**: 12
- **Tags**: reflexion, logic_error

### [REFLEXION] The arbitrage strategy entered a position at 0.403 but exited at 0.426, realizing a loss despite ...

Root cause: The arbitrage strategy entered a position at 0.403 but exited at 0.426, realizing a loss despite a price increase, indicating the strategy failed to identify or maintain a profitable arbitrage spread before execution or the spread collapsed post-entry due to market inefficiency or timing misalignment.
Error class: logic_error
Counterfactual: N/A
Learning: Combinatorial arbitrage requires real-time spread re-validation immediately before trade execution, not just at discovery; a 30-se...

- **Importance**: 0.75 | **Confidence**: 1.00 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 2341 | **Injections**: 12
- **Tags**: reflexion, logic_error

### [REFLEXION] The LLM probability model generated a buy signal at 0.624 that lacked sufficient edge to overcome...

Root cause: The LLM probability model generated a buy signal at 0.624 that lacked sufficient edge to overcome transaction costs and market microstructure, resulting in a loss when the position exited at 0.627 despite a favorable price movement.
Error class: logic_error
Counterfactual: The edge_threshold_pct (3.0%) should have been increased or the confidence_threshold (0.3) should have been raised to filter out marginal signals; alternatively, the RAG context retrieval (top_k=5) may have been in...

- **Importance**: 0.75 | **Confidence**: 1.00 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 1322 | **Injections**: 13
- **Tags**: reflexion, logic_error

### [REFLEXION] The LLM probability model generated a buy signal at 0.632 that lacked sufficient edge, as the sub...

Root cause: The LLM probability model generated a buy signal at 0.632 that lacked sufficient edge, as the subsequent price movement to 0.636 (+0.63%) failed to overcome the 3% edge threshold requirement, indicating either poor probability calibration or inadequate RAG context for this specific pair.
Error class: logic_error
Counterfactual: The agent should have either (1) rejected the trade signal because the predicted edge (0.63%) fell below the edge_threshold_pct (3.0%), or (2) increased the c...

- **Importance**: 0.75 | **Confidence**: 1.00 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 763 | **Injections**: 13
- **Tags**: reflexion, logic_error

### [REFLEXION] The exit price (0.014) represents a 96.6% drawdown from entry (0.41), indicating the stop-loss me...

Root cause: The exit price (0.014) represents a 96.6% drawdown from entry (0.41), indicating the stop-loss mechanism failed to execute at the configured 5% threshold, likely due to a gap down or liquidity failure on the PM61A130CE/YES pair.
Error class: logic_error
Counterfactual: Implement hard stops with order placement at entry time rather than reactive monitoring, add circuit breakers to halt trading on pairs with insufficient liquidity or extreme volatility, and validate that the pair suppo...

- **Importance**: 0.75 | **Confidence**: 1.00 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 675 | **Injections**: 12
- **Tags**: reflexion, logic_error

### [REFLEXION] The arbitrage spread between entry (0.64255745) and exit (0.642) of -0.00055745 fell below the mi...

Root cause: The arbitrage spread between entry (0.64255745) and exit (0.642) of -0.00055745 fell below the minimum required spread threshold of 1.0%, resulting in a loss rather than the expected profitable arbitrage execution.
Error class: logic_error
Counterfactual: The agent should have either (1) rejected the trade opportunity before execution since the identifiable spread was insufficient to cover transaction costs and slippage, or (2) dynamically adjusted the min_arb_spread_pct parameter ba...

- **Importance**: 0.75 | **Confidence**: 1.00 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 437 | **Injections**: 12
- **Tags**: reflexion, logic_error

### [REFLEXION] The arbitrage strategy entered a position at 0.4304 but exited at 0.445, resulting in a loss desp...

Root cause: The arbitrage strategy entered a position at 0.4304 but exited at 0.445, resulting in a loss despite a price increase, suggesting the strategy either reversed positions incorrectly, paid excessive fees/slippage that exceeded the 1% minimum spread threshold, or executed against stale market data.
Error class: logic_error
Counterfactual: The agent should have: (1) validated that the identified arbitrage spread exceeded fees+slippage before entry, (2) implemented real-time spread monito...

- **Importance**: 0.75 | **Confidence**: 1.00 | **Effectiveness**: 1.00
- **Scope**: team | **Access count**: 2160 | **Injections**: 12
- **Tags**: reflexion, logic_error

### [REFLEXION] The mean reversion strategy entered a position at z_score=2.0 during what appeared to be an overs...

Root cause: The mean reversion strategy entered a position at z_score=2.0 during what appeared to be an oversold condition, but the asset continued declining past the stop-loss threshold, indicating the price movement was a trend reversal rather than a mean reversion opportunity.
Error class: logic_error
Counterfactual: The agent should have validated mean reversion conditions more rigorously by confirming: (1) sufficient historical volatility mean to revert toward (lookback_periods=5 is too sho...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The arbitrage opportunity identified had insufficient margin of safety; the 1.0% minimum spread t...

Root cause: The arbitrage opportunity identified had insufficient margin of safety; the 1.0% minimum spread threshold was too low to cover transaction costs, slippage, and market microstructure noise, resulting in a realized loss despite a theoretically profitable setup.
Error class: logic_error
Counterfactual: N/A
Learning: Combinatorial arbitrage profitability depends critically on the spread-to-cost ratio, not just spread detection. Future iterations should model expected transaction costs (f...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 686 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The ensemble's consensus mechanism (threshold 0.3) was too permissive, allowing a weak bullish si...

Root cause: The ensemble's consensus mechanism (threshold 0.3) was too permissive, allowing a weak bullish signal to override the risk manager's warnings, resulting in a long position entered at 0.6328 that exited at a loss when price moved to 0.639.
Error class: logic_error
Counterfactual: Increase consensus_threshold to 0.6-0.7 to require stronger agreement across agents, implement a veto mechanism for the risk_manager role when volatility or drawdown thresholds are exceeded, and reduce positi...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 29 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The momentum strategy entered a position that failed to achieve the 3% take profit target before ...

Root cause: The momentum strategy entered a position that failed to achieve the 3% take profit target before hitting adverse price movement, resulting in a loss that exceeded the acceptable threshold despite the 5% stop loss parameter being in place.
Error class: logic_error
Counterfactual: The agent should have either (1) validated that recent momentum indicators were sufficiently strong before entry to justify the risk/reward ratio, (2) reduced position size given the volatile nature of this p...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The agent executed a combinatorial arbitrage trade with insufficient spread, resulting in a loss ...

Root cause: The agent executed a combinatorial arbitrage trade with insufficient spread, resulting in a loss due to adverse price movement between entry and exit.
Error class: logic_error
Counterfactual: The agent should have used a higher minimum arbitrage spread threshold or incorporated more robust price movement prediction to avoid entering unprofitable trades.
Learning: Ensure that arbitrage strategies account for potential slippage and market volatility by setting more conservative spread...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 2710 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The LLM probability model generated an incorrect directional prediction, entering long at 0.319 w...

Root cause: The LLM probability model generated an incorrect directional prediction, entering long at 0.319 when the market moved against the position to 0.331, resulting in a loss despite the 3% edge threshold being met at entry.
Error class: logic_error
Counterfactual: Implement a pre-trade validation layer that backtests the LLM's probability calibration on historical data for this specific pair before deploying live trades, or reduce position_size_pct from 5% to 1-2% to limit downside exposu...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 2017 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The arbitrage spread of approximately 0.76% (entry 0.44665582 → exit 0.443) fell below the minimu...

Root cause: The arbitrage spread of approximately 0.76% (entry 0.44665582 → exit 0.443) fell below the minimum required threshold of 1.0%, resulting in a loss when accounting for trading fees and slippage.
Error class: logic_error
Counterfactual: The agent should have either (1) rejected this trade opportunity before execution since the observable spread was below min_arb_spread_pct, or (2) dynamically adjusted the minimum spread threshold based on actual market microstructure data and fee struc...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 1091 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The ensemble agent's weighted decision framework (fundamentals: 1.5, technical: 1.2, risk_manager...

Root cause: The ensemble agent's weighted decision framework (fundamentals: 1.5, technical: 1.2, risk_manager: 1.3) generated a bullish signal that failed to account for adverse price movement, resulting in a loss despite low confidence thresholds (0.3) that should have triggered position rejection or sizing reduction.
Error class: logic_error
Counterfactual: Implement a pre-trade validation gate that requires consensus_threshold agreement across risk_manager and trader agents before entry, redu...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 2334 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The stop loss was triggered at -4.52% loss, indicating the price moved against the position beyon...

Root cause: The stop loss was triggered at -4.52% loss, indicating the price moved against the position beyond the configured 4.0% stop loss threshold, suggesting either insufficient mean reversion confirmation before entry or a market regime shift that violated the mean reversion assumption.
Error class: logic_error
Counterfactual: The agent should have validated that the z-score entry signal (2.0) was sufficiently strong relative to recent volatility, implemented a tighter initial position siz...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 0 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The LLM probability model generated a buy signal at 0.385 that lacked sufficient edge to overcome...

Root cause: The LLM probability model generated a buy signal at 0.385 that lacked sufficient edge to overcome transaction costs and market microstructure, resulting in an immediate adverse price movement to 0.396 before the position could be exited profitably.
Error class: logic_error
Counterfactual: The agent should have either (1) increased the edge_threshold_pct from 3.0% to a higher value reflecting true expected return after costs, (2) validated the LLM's probability estimate against the ac...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 2900 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The LLM probability model generated a buy signal at 0.431 that failed to achieve the predicted ed...

Root cause: The LLM probability model generated a buy signal at 0.431 that failed to achieve the predicted edge threshold of 3.0%, resulting in a loss when the position exited at 0.45 despite price movement in the predicted direction.
Error class: logic_error
Counterfactual: The agent should have either (1) implemented stricter confidence validation before entry by requiring the LLM's confidence score to exceed the 0.3 threshold by a larger margin, (2) used the RAG context to identify market reg...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 2012 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The agent ensemble's role weighting and consensus thresholds led to an overconfident long positio...

Root cause: The agent ensemble's role weighting and consensus thresholds led to an overconfident long position despite insufficient margin for error, resulting in a realized loss when the price moved slightly against the position.
Error class: logic_error
Counterfactual: The agent should have used more conservative role weights and/or higher consensus and confidence thresholds to avoid entering a marginal trade with limited upside and higher risk.
Learning: Carefully calibrate ensemble role weig...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 6356 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The arbitrage spread of approximately 0.33 basis points (0.0233533 on a 0.62323533 entry) fell be...

Root cause: The arbitrage spread of approximately 0.33 basis points (0.0233533 on a 0.62323533 entry) fell below the configured 1.0% minimum threshold, yet the trade was executed anyway, indicating a validation bypass or threshold miscalibration.
Error class: logic_error
Counterfactual: Implement a hard pre-execution guard that blocks trades when detected spread < min_arb_spread_pct, and add logging to surface why the LLM validation (use_llm_validation:true) approved a sub-threshold opportunity....

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 22 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The agent executed a combinatorial arbitrage trade based on insufficient spread or unreliable val...

Root cause: The agent executed a combinatorial arbitrage trade based on insufficient spread or unreliable validation, resulting in a loss when the price moved unfavorably before exit.
Error class: logic_error
Counterfactual: The agent should have used a higher minimum arbitrage spread threshold or more robust validation to avoid low-confidence trades.
Learning: Ensure that arbitrage strategies incorporate stricter spread thresholds and more reliable validation mechanisms to minimize exposure to...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 17 | **Injections**: 0
- **Tags**: reflexion, logic_error

### [REFLEXION] The ensemble's risk management agent failed to prevent a loss-making trade, likely due to insuffi...

Root cause: The ensemble's risk management agent failed to prevent a loss-making trade, likely due to insufficient weighting of risk constraints relative to bullish sentiment/technical signals in the consensus mechanism.
Error class: logic_error
Counterfactual: The risk_manager role weight (1.3) should have been elevated above sentiment (1.0) and news (0.8) weights, or a hard stop-loss rule should have been enforced before trade execution rather than relying on soft consensus thresholds (0.3 con...

- **Importance**: 0.75 | **Confidence**: 0.60 | **Effectiveness**: -
- **Scope**: team | **Access count**: 3347 | **Injections**: 0
- **Tags**: reflexion, logic_error

