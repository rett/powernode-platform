# Shared Knowledge

> Auto-generated by `rails mcp:sync_docs` on 2026-02-22 00:14 UTC
> **Do not edit manually** — changes will be overwritten on next sync.
> Source: `ai_shared_knowledges` | Filter: quality_score >= 0.5

---

**9 entries** exported (max 100)

## Markdown (1)

### TestApp API Endpoints Reference

# TestApp API Endpoints

## Health
- GET /api/health - Service health check

## Users
- GET /api/users - List users
- POST /api/users - Create user
- GET /api/users/:id - Get user

## Projects
- GET /api/projects - List projects
- POST /api/projects - Create project

- **Quality**: 0.56 | **Usage**: 2 | **Access level**: team
- **Source**: agent | **Tags**: testapp,api,endpoints

## Snippet (1)

### Cosine distance for semantic search

pgvector cosine distance: nearest_neighbors(:embedding, v, distance: 'cosine'). neighbor_distance: 0=identical, 2=opposite. Similarity = 1 - distance. Threshold: where('neighbor_distance <= ?', 1.0 - threshold).

- **Quality**: 0.62 | **Usage**: 2 | **Access level**: account
- **Source**: manual | **Tags**: ai, pgvector, search

## Procedure (7)

### MCP tool action_definitions pattern

## MCP Tool Action Definitions Pattern

Multi-action tools (e.g., AgentManagementTool, SkillTool) expose multiple MCP tools from a single Ruby class. The `action_definitions` class method returns per-action schemas:

```ruby
def self.action_definitions
  {
    "list_agents" => { description: "List all agents", parameters: {} },
    "create_agent" => { description: "Create agent", parameters: { name: { type: :string, required: true } } }
  }
end
```

**Key rules:**
- BaseTool default strips `:act...

- **Quality**: 0.70 | **Usage**: 1 | **Access level**: account
- **Source**: manual | **Tags**: mcp, tools, pattern, action-definitions

### Knowledge quality lifecycle operations

## Knowledge Quality Lifecycle

### Compound Learnings
- verify!(user:) sets status: verified, confidence +0.1, importance +15%
- disprove!(user:, reason:) sets status: disproven, importance: 0.05, confidence: 0.1
- resolve_contradiction!(note:) records resolution timestamp and note
- Auto-decay: >7 days old, importance reduced
- Auto-archive: >30 days + importance < 0.1

### Shared Knowledge
- record_rating!(1-5) updates running average, triggers quality recalc
- Quality = structural(30%) + usa...

- **Quality**: 0.68 | **Usage**: 1 | **Access level**: account
- **Source**: manual | **Tags**: ai, knowledge-quality, lifecycle, compound-learning

### Permission-based access control never roles

Frontend: currentUser?.permissions?.includes('users.manage'). Backend: current_user.has_permission?('name') — never permissions.include?() (returns objects). Never check roles for access control decisions.

- **Quality**: 0.56 | **Usage**: 1 | **Access level**: account
- **Source**: manual | **Tags**: security, permissions, frontend, backend

### Systemd service management

Services via systemd template units: powernode-backend@, powernode-worker@, powernode-frontend@, powernode.target. Install: sudo scripts/systemd/powernode-installer.sh install. Never use manual commands (rails server, sidekiq, npm start).

- **Quality**: 0.56 | **Usage**: 1 | **Access level**: account
- **Source**: manual | **Tags**: infrastructure, systemd

### Mandatory render_success and render_error

All Rails API controllers must use render_success() and render_error() for responses. Never use render json: directly. These helpers ensure consistent API response format.

- **Quality**: 0.55 | **Usage**: 1 | **Access level**: account
- **Source**: manual | **Tags**: backend, controller, api

### Theme classes only for styling

Frontend must use theme-aware CSS classes: bg-theme-*, text-theme-*, border-theme-*. Never hardcode colors. Ensures dark mode and custom themes work correctly.

- **Quality**: 0.55 | **Usage**: 1 | **Access level**: account
- **Source**: manual | **Tags**: frontend, css, theme

### Namespace foreign key prefixes

FK naming: Ai:: → ai_ (ai_agent_id), Devops:: → ci_cd_ (ci_cd_pipeline_id), BaaS:: → baas_ (baas_customer_id). Others: explicit FK or omit if unambiguous.

- **Quality**: 0.55 | **Usage**: 1 | **Access level**: account
- **Source**: manual | **Tags**: migration, database, naming

