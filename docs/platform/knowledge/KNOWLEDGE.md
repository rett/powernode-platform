# Shared Knowledge

> Auto-generated by `rails mcp:sync_docs` on 2026-02-26 12:09 UTC
> **Do not edit manually** — changes will be overwritten on next sync.
> Source: `ai_shared_knowledges` | Filter: quality_score >= 0.5

---

**85 entries** exported (max 100)

## Text (1)

### Code Quality Violations Registry - February 2026 Audit

Code quality audit baseline from 2026-02-25. OVERSIZED CONTROLLERS (18 exceed 300-line limit): reverse_proxy_controller 910, analytics_controller 808, reports_controller 664, workers_controller 598, streamable_http_controller 587, conversations_controller 560, maintenance_controller 547, webhooks_controller 542, internal/reverse_proxy_controller 531, skill_graph_controller 524, repositories_controller 514, providers_controller 486, files_controller 451, conversation_management_actions 438, admin...

- **Quality**: 0.65 | **Usage**: 16 | **Access level**: team
- **Source**: agent | **Tags**: audit, code-quality, technical-debt

## Markdown (1)

### TestApp API Endpoints Reference

# TestApp API Endpoints

## Health
- GET /api/health - Service health check

## Users
- GET /api/users - List users
- POST /api/users - Create user
- GET /api/users/:id - Get user

## Projects
- GET /api/projects - List projects
- POST /api/projects - Create project

- **Quality**: 0.51 | **Usage**: 18 | **Access level**: team
- **Source**: agent | **Tags**: testapp,api,endpoints

## Code (1)

### Gitea Actions Health Check Workflow Template

## Gitea Actions Health Check Workflow Template

Reusable CI workflow for automated health checks in Gitea repositories.

### Workflow File: `.gitea/workflows/health-check.yml`

```yaml
name: Health Check
on:
  push:
    branches: [main, master]
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run health check
        run: |
          chmod +x scripts/check-health.sh
          ./s...

- **Quality**: 0.65 | **Usage**: 12 | **Access level**: team
- **Source**: agent | **Tags**: gitea, ci-cd, health-check, template, smoke-test

## Snippet (6)

### AI test matchers

Custom matchers: be_a_valid_ai_response, have_execution_status(:status), create_audit_log(:action). In spec/support/ai_matchers.rb.

- **Quality**: 0.65 | **Usage**: 36 | **Access level**: account
- **Source**: manual | **Tags**: testing, rspec, ai

### Cosine distance for semantic search

pgvector cosine distance: nearest_neighbors(:embedding, v, distance: 'cosine'). neighbor_distance: 0=identical, 2=opposite. Similarity = 1 - distance. Threshold: where('neighbor_distance <= ?', 1.0 - threshold).

- **Quality**: 0.65 | **Usage**: 6 | **Access level**: account
- **Source**: manual | **Tags**: ai, pgvector, search

### Shared examples for common test patterns

Shared examples: 'requires authentication', 'requires permission', 'scopes to current account'. In spec/support/shared_examples/.

- **Quality**: 0.64 | **Usage**: 25 | **Access level**: account
- **Source**: manual | **Tags**: testing, rspec, shared-examples

### user_with_permissions test helper

Create test users: user_with_permissions('perm.name') from permission_test_helpers.rb. Creates user, role, assigns permission. Never create users manually.

- **Quality**: 0.62 | **Usage**: 12 | **Access level**: account
- **Source**: manual | **Tags**: testing, rspec, helpers

### auth_headers_for test helper

Auth headers: auth_headers_for(user) returns { Authorization: 'Bearer ...' }. Use in all authenticated request specs.

- **Quality**: 0.62 | **Usage**: 13 | **Access level**: account
- **Source**: manual | **Tags**: testing, rspec, authentication

### Factory traits for model states

Factories support :active, :paused, :archived traits. AI factories in spec/factories/ai/.

- **Quality**: 0.58 | **Usage**: 8 | **Access level**: account
- **Source**: manual | **Tags**: testing, factories, rspec

## Procedure (48)

### Workspace message flow: ActionCable → SSE → Agent

Complete workspace message pipeline:

1. User sends message in workspace UI
2. AiConversationChannel#send_message creates user message, broadcasts message_created event via ActionCable with conversation_id, workspace name, serialized message
3. SSE stream action (streamable_http_controller.rb) subscribes to workspace channels for the connected agent
4. Mention filter (lines 142-161): message_created and ai_response_complete only forwarded if connected agent is @mentioned (structural metadata men...

- **Quality**: 0.80 | **Usage**: 24 | **Access level**: team
- **Source**: agent | **Tags**: workspace, sse, actioncable, agent, message-flow, dispatch, concierge, mentions

### MCP tool action_definitions pattern

## MCP Tool Action Definitions Pattern

Multi-action tools (e.g., AgentManagementTool, SkillTool) expose multiple MCP tools from a single Ruby class. The `action_definitions` class method returns per-action schemas:

```ruby
def self.action_definitions
  {
    "list_agents" => { description: "List all agents", parameters: {} },
    "create_agent" => { description: "Create agent", parameters: { name: { type: :string, required: true } } }
  }
end
```

**Key rules:**
- BaseTool default strips `:act...

- **Quality**: 0.80 | **Usage**: 9 | **Access level**: account
- **Source**: manual | **Tags**: mcp, tools, pattern, action-definitions

### Knowledge Quality Maintenance Runbook

## Knowledge Quality Maintenance Runbook

### Daily Automated Cascade
| Time | Job | Effect |
|------|-----|--------|
| 3:45 AM | CompoundLearning decay | Exponential importance decay on stale learnings |
| 4:00 AM | Memory consolidation | STM→LTM promotion (access>=3), dedup (similarity>=0.92) |
| 4:15 AM | Skill lifecycle | Conflict scan, stale decay, re-embedding |
| 4:30 AM | SharedKnowledge maintenance | Import from learnings, recalculate quality scores |
| 4:45 AM | KnowledgeGraph maintena...

- **Quality**: 0.80 | **Usage**: 9 | **Access level**: team
- **Source**: agent | **Tags**: knowledge, maintenance, quality

### Extension Development Guide

## Extension Development Guide

### Creating a New Extension

1. **Create directory**: `extensions/my-extension/`
2. **Create Rails Engine**: `lib/my_extension/engine.rb` inheriting `Rails::Engine`
3. **Create manifest**: `extension.json` declaring routes, models, feature flags
4. **Register**: Call `ExtensionRegistry.register(:my_extension)` in engine initializer
5. **Backend decorators**: Use `AccountDecorator` pattern to add associations to core models
6. **Frontend**: Create extension loader...

- **Quality**: 0.80 | **Usage**: 8 | **Access level**: team
- **Source**: agent | **Tags**: extensions, rails-engine, architecture, feature-gate, extraction-guide

### MCP Tool Development Procedure

## MCP Tool Development Procedure

### Step 1: Create Tool Class
```ruby
# server/app/services/ai/tools/my_tool.rb
module Ai::Tools
  class MyTool < BaseTool
    def self.action_definitions
      {
        "my_action" => {
          description: "Description for MCP schema",
          parameters: { param1: { type: :string, required: true } }
        }
      }
    end

    def execute(params:)
      case params[:action]
      when "my_action" then handle_my_action(params)
      end
    end
  end...

- **Quality**: 0.79 | **Usage**: 7 | **Access level**: team
- **Source**: agent | **Tags**: mcp, tools, development

### Namespace foreign key prefixes

FK naming: Ai:: uses ai_ prefix (ai_agent_id), Devops:: uses devops_ prefix (devops_pipeline_id), BaaS:: uses baas_ prefix (baas_customer_id). Others: explicit FK or omit if unambiguous. Always pair class_name: with foreign_key: in associations.

- **Quality**: 0.77 | **Usage**: 13 | **Access level**: account
- **Source**: manual | **Tags**: migration, database, naming

### DevOps Subsystem Reference

DevOps Subsystem: ~41 models in Devops:: namespace. Pipeline > PipelineStep > PipelineRun. StepHandlerRegistry maps step types to handlers (build, test, deploy, review, scan). GitProvider integrates GitHub, Gitea, and GitLab. RunnerDispatchService dispatches to GitHub/Gitea Actions. GiteaApiClient (1014 lines) with resolve_ref fix for slashed branch names. GitLabApiClient (981 lines). Docker Swarm: DockerSwarmCluster + DockerSwarmService. PipelineIntelligenceService for analytics. Gotchas: compa...

- **Quality**: 0.77 | **Usage**: 16 | **Access level**: team
- **Source**: agent | **Tags**: devops, pipelines, git, containers

### Feature Gating Implementation Guide

## Feature Gating Implementation Guide

### Adding a Feature Gate

#### Backend (Controller)
```ruby
class Api::V1::MyController < ApplicationController
  before_action :require_enterprise!, only: [:billing_action]
  
  def billing_action
    return render_error('Feature not available') unless FeatureGateService.feature_enabled?(:my_feature)
    # ...
  end
end
```

#### Backend (Service)
```ruby
return unless Shared::FeatureGateService.enterprise_loaded?
return unless Flipper.enabled?(:my_granu...

- **Quality**: 0.76 | **Usage**: 7 | **Access level**: team
- **Source**: agent | **Tags**: feature-gating, extensions, flipper

### Platform Architecture Overview

Powernode Platform Architecture: Rails 8.1 API backend (server/) with JWT auth, UUIDv7 PKs, 285 controllers, 560+ services, 354 tables, 261 models across 13 namespaces. React 19.1 TypeScript frontend (frontend/) with Tailwind CSS v4.1, Vite 7.2, theme-aware styling, 11 feature modules (account, admin, ai, baas, business, content, delegations, developer, devops, missions, privacy). Sidekiq standalone worker (worker/) with 163 jobs communicating via HTTP API. PostgreSQL with pgvector, Redis for ca...

- **Quality**: 0.76 | **Usage**: 34 | **Access level**: team
- **Source**: agent | **Tags**: architecture, overview, platform

### WebSocket Channel Patterns

17 ActionCable channels in server/app/channels/. AI Execution (6): AiOrchestrationChannel (unified AI ops), AiAgentExecutionChannel (execution monitoring by execution_id), AiStreamingChannel (token streaming from providers), AiConversationChannel (AI conversation messaging), AiWorkflowMonitoringChannel (monitoring and alerts), AiWorkflowOrchestrationChannel (workflow operations). Team (2): TeamChannelChannel (message streaming by channel_id), TeamExecutionChannel (execution events: started, prog...

- **Quality**: 0.76 | **Usage**: 25 | **Access level**: team
- **Source**: agent | **Tags**: websocket, actioncable, frontend

### AI Agent System Reference

AI Agent System: 127 models in Ai:: namespace, 305+ services. Trust tiers: supervised(0.0), monitored(0.4), trusted(0.7), autonomous(0.9). Memory tiers: working(Redis), short_term(PG+TTL), long_term(pgvector), shared(pgvector+ACL). Teams: AgentTeam with 6 role profiles, 4 strategies (sequential, parallel, hierarchical, mesh). Protocols: A2A (JSON-RPC 2.0), ACP (8 endpoints). Missions: AiMission, RalphLoop, RalphTask with 12-stage pipeline. Key services: ConciergeService, MissionService, CodeFact...

- **Quality**: 0.76 | **Usage**: 25 | **Access level**: team
- **Source**: agent | **Tags**: ai, agents, orchestration

### Knowledge quality lifecycle operations

## Knowledge Quality Lifecycle

### Compound Learnings
- verify!(user:) sets status: verified, confidence +0.1, importance +15%
- disprove!(user:, reason:) sets status: disproven, importance: 0.05, confidence: 0.1
- resolve_contradiction!(note:) records resolution timestamp and note
- Auto-decay: >7 days old, importance reduced
- Auto-archive: >30 days + importance < 0.1

### Shared Knowledge
- record_rating!(1-5) updates running average, triggers quality recalc
- Quality = structural(30%) + usa...

- **Quality**: 0.74 | **Usage**: 8 | **Access level**: account
- **Source**: manual | **Tags**: ai, knowledge-quality, lifecycle, compound-learning

### LLM Proxy Integration Guide: 10-Step E2E Flow

Step-by-step guide for the worker-to-provider LLM call chain.

1. WORKER JOB: Include AiLlmProxyConcern in your job class. This provides the llm_proxy method.
   File: worker/app/jobs/concerns/ai_llm_proxy_concern.rb

2. PROXY CLIENT: Call llm_proxy.execute_tool_loop(agent_id:, messages:, model:) (or complete, complete_with_tools, etc). LlmProxyClient is initialized with backend_api_post method reference.
   File: worker/app/services/llm_proxy_client.rb

3. HTTP POST: LlmProxyClient sends POST t...

- **Quality**: 0.74 | **Usage**: 12 | **Access level**: team
- **Source**: agent | **Tags**: ai, llm, proxy, integration, worker

### Team Execution Pipeline E2E Flow

Complete team execution pipeline from client dispatch through strategy execution to completion.

## Pipeline Steps

1. Client dispatches team execution via MCP execute_team action or API POST /api/v1/ai/teams/:id/execute
2. Controller creates Ai::TeamExecution record (status: pending) linked to team and account
3. AiTeamExecutionJob is dispatched to worker via Sidekiq (queue: ai_team)
4. Worker job fetches team config, members, and credentials from server API
5. StrategyFactory.build selects str...

- **Quality**: 0.74 | **Usage**: 9 | **Access level**: team
- **Source**: agent | **Tags**: none

### API Response Standards Reference

Mandatory: render_success(data) and render_error(message, status). Never render json: directly. Format: { success: true, data: ... } or { success: false, error: { message: ... } }. Controllers: Api::V1 namespace, inherit ApplicationController, max 300 lines. Always .includes() for associations. Webhook receivers must return 200/202 on errors (never 500). Auth: JWT via Authorization Bearer (main API), Doorkeeper OAuth 2.1 (MCP). Permission: current_user.has_permission?(name), never permissions.in...

- **Quality**: 0.73 | **Usage**: 21 | **Access level**: team
- **Source**: agent | **Tags**: api, controllers, standards

### Test Patterns Quick Reference

RSpec: bundle exec rspec --format progress. Helpers: user_with_permissions('perm'), auth_headers_for(user), json_response, expect_success_response, expect_error_response. Shared examples: requires authentication, requires permission, scopes to current account. DatabaseCleaner with :deletion (not truncation). Single-process rspec only. Frontend: CI=true npm test. E2E: Page objects in e2e/pages/ai/, selectors: data-testid → class pattern → getByRole. AI matchers: be_a_valid_ai_response, have_execu...

- **Quality**: 0.72 | **Usage**: 32 | **Access level**: team
- **Source**: agent | **Tags**: testing, rspec, vitest

### Autonomy Service Map: ExecutionGate, CapabilityMatrix, TrustEngine, and 18 More

Service map for 4 AI subsystems in Powernode.

AUTONOMY (12 services at server/app/services/ai/autonomy/):
- ExecutionGateService: 5-check fail-closed gate (capability, budget, conformance, anomaly, trust freshness)
- CapabilityMatrixService: 4 tiers (supervised, monitored, trusted, autonomous) x 10 actions
- TrustEngineService: 5-dimension weighted score (safety 0.30, reliability 0.25, quality 0.20, cost_efficiency 0.15, speed 0.10)
- BehavioralFingerprintService: Z-score anomaly detection on e...

- **Quality**: 0.70 | **Usage**: 11 | **Access level**: team
- **Source**: agent | **Tags**: ai, autonomy, service-map, architecture

### Permission-based access control never roles

Frontend: currentUser?.permissions?.includes('users.manage'). Backend: current_user.has_permission?('name') — never permissions.include?() (returns objects). Never check roles for access control decisions.

- **Quality**: 0.66 | **Usage**: 13 | **Access level**: account
- **Source**: manual | **Tags**: security, permissions, frontend, backend

### Project Health Dashboard Feature Spec

## Project Health Dashboard Feature Spec

### Overview
Add a Project Health Dashboard feature to the ipnode/testapp Gitea repository.

### Implementation Status: COMPLETED
- PR #6 created on branch feature/project-health-dashboard
- All 4 files created/updated successfully
- 3 Ollama agents and 1 team created for the feature

### Files
1. `health.json` - JSON Schema 2020-12 for health metrics
2. `scripts/check-health.sh` - Bash health check script (exits 0=healthy, 1=unhealthy)
3. `.gitea/workfl...

- **Quality**: 0.65 | **Usage**: 11 | **Access level**: account
- **Source**: agent | **Tags**: smoke-test, testapp, health-dashboard, gitea, completed

### Mandatory render_success and render_error

All Rails API controllers must use render_success() and render_error() for responses. Never use render json: directly. These helpers ensure consistent API response format.

- **Quality**: 0.65 | **Usage**: 11 | **Access level**: account
- **Source**: manual | **Tags**: backend, controller, api

### Systemd service management

Services via systemd template units: powernode-backend@, powernode-worker@, powernode-frontend@, powernode.target. Install: sudo scripts/systemd/powernode-installer.sh install. Never use manual commands (rails server, sidekiq, npm start).

- **Quality**: 0.65 | **Usage**: 16 | **Access level**: account
- **Source**: manual | **Tags**: infrastructure, systemd

### Namespaced model class_name uses double colon

Use :: separator in class_name: — 'Ai::AgentTeam' not 'AiAgentTeam'. Rails won't find the class without ::.

- **Quality**: 0.63 | **Usage**: 7 | **Access level**: account
- **Source**: manual | **Tags**: migration, activerecord, naming

### Theme classes only for styling

Frontend must use theme-aware CSS classes: bg-theme-*, text-theme-*, border-theme-*. Never hardcode colors. Ensures dark mode and custom themes work correctly.

- **Quality**: 0.63 | **Usage**: 7 | **Access level**: account
- **Source**: manual | **Tags**: frontend, css, theme

### CI=true for frontend tests

Frontend tests: cd frontend && CI=true npm test. Non-interactive mode for consistent CI behavior.

- **Quality**: 0.63 | **Usage**: 19 | **Access level**: account
- **Source**: manual | **Tags**: testing, frontend, ci

### Controller size limit 300 lines

Rails controllers: max 300 lines. Extract query logic to services, serialization to concerns. Keep controllers focused on request handling.

- **Quality**: 0.63 | **Usage**: 8 | **Access level**: account
- **Source**: manual | **Tags**: backend, controller, architecture

### BaseAiService concern for AI services

AI services include BaseAiService concern for common functionality: account scoping, error handling, logging. At server/app/services/ai/concerns/.

- **Quality**: 0.62 | **Usage**: 15 | **Access level**: account
- **Source**: manual | **Tags**: ai, service, pattern

### Path aliases for cross-feature imports

Frontend: @/shared/ for shared utilities, @/features/ for feature code. Enterprise: @enterprise/ for intra-enterprise imports.

- **Quality**: 0.62 | **Usage**: 17 | **Access level**: account
- **Source**: manual | **Tags**: frontend, imports

### data-testid selectors for E2E

E2E selector priority: data-testid first, class*="pattern" second, getByRole third. Add data-testid to new components.

- **Quality**: 0.61 | **Usage**: 11 | **Access level**: account
- **Source**: manual | **Tags**: testing, e2e, selectors

### E2E page object model pattern

E2E tests use page objects in e2e/pages/ and e2e/pages/ai/. Page objects encapsulate selectors and interactions. Always use existing ones.

- **Quality**: 0.61 | **Usage**: 12 | **Access level**: account
- **Source**: manual | **Tags**: testing, e2e, playwright

### Global notifications only no local state

Frontend success/error feedback uses global notification system only. Never local component state for success/error messages.

- **Quality**: 0.60 | **Usage**: 11 | **Access level**: account
- **Source**: manual | **Tags**: frontend, ux

### Feature flags via Flipper for AI features

AI flags via Flipper: :compound_learning_injection, :compound_learning_promotion. Extraction always runs regardless of flags.

- **Quality**: 0.60 | **Usage**: 7 | **Access level**: account
- **Source**: manual | **Tags**: ai, feature-flags, flipper

### Frozen string literal pragma required

Every Ruby file must begin with `# frozen_string_literal: true`. Improves performance and prevents accidental string mutation.

- **Quality**: 0.59 | **Usage**: 7 | **Access level**: account
- **Source**: manual | **Tags**: backend, ruby, style

### Webhook receivers return 200 on errors

Inbound webhook endpoints return 200 or 202 on processing errors — never 500. 500 causes provider retry storms (Stripe, PayPal will retry repeatedly).

- **Quality**: 0.59 | **Usage**: 6 | **Access level**: account
- **Source**: manual | **Tags**: backend, webhooks, api

### Always pair class_name with foreign_key

Associations with class_name: must also specify foreign_key:. Example: belongs_to :provider, class_name: 'Ai::Provider', foreign_key: 'ai_provider_id'.

- **Quality**: 0.59 | **Usage**: 4 | **Access level**: account
- **Source**: manual | **Tags**: migration, associations, activerecord

### Worker BaseJob Pattern: Inheritance and Lifecycle

All worker jobs (worker/app/jobs/) inherit BaseJob and implement execute(). BaseJob includes Sidekiq::Job with retry 3, dead true, queue default. Features: exponential backoff retry (API errors [30,60,180]s, others count^4+15+rand), runaway loop detection (5/min auto-disables 5 min), idempotency helpers via Redis (already_processed?/mark_processed), structured logging (log_info/log_error/log_warn), metrics tracking via Redis (1000 cap, 24h TTL), execution timing. Key methods: execute MUST overri...

- **Quality**: 0.58 | **Usage**: 0 | **Access level**: team
- **Source**: agent | **Tags**: none

### Worker API Communication: 4 HTTP Clients and Auth

Worker communicates with server via HTTP only. 4 API clients in worker/app/services/: BackendApiClient (primary, WORKER_SERVICE_TOKEN Bearer auth, get/post/put/patch/delete, raises ApiError), ApiClient (base HTTP client with pooling), WebAuthApiClient (Sidekiq Web UI auth via backend), LlmProxyClient (AI LLM calls via server proxy). Circuit breakers: AI provider 600s (with_ai_provider_circuit_breaker in ai_jobs_concern.rb), backend API 120s (with_backend_api_circuit_breaker). State in Redis.

- **Quality**: 0.58 | **Usage**: 0 | **Access level**: team
- **Source**: agent | **Tags**: none

### JSON columns use lambda defaults

ActiveRecord JSON columns: `attribute :config, :json, default: -> { {} }`. Never `default: {}` — shares mutable object across instances.

- **Quality**: 0.58 | **Usage**: 4 | **Access level**: account
- **Source**: manual | **Tags**: backend, database, activerecord

### Eager loading with includes required

Always .includes() when iterating associations. Never bare .all followed by .map/.each accessing relations without eager loading.

- **Quality**: 0.58 | **Usage**: 6 | **Access level**: account
- **Source**: manual | **Tags**: backend, performance, database

### Flat navigation structure

Frontend navigation: flat structure, no nested submenus. Each nav item links directly to a page. Sub-navigation within pages via sections.

- **Quality**: 0.58 | **Usage**: 5 | **Access level**: account
- **Source**: manual | **Tags**: frontend, navigation

### Rails.logger only no puts or print

Backend: Rails.logger (info, warn, error, debug). Never puts, print, or p in production code.

- **Quality**: 0.58 | **Usage**: 8 | **Access level**: account
- **Source**: manual | **Tags**: backend, logging

### No console.log in production frontend

Use logger utility: import { logger } from '@/shared/utils/logger'. Never use console.log in production. Logger respects environment settings.

- **Quality**: 0.58 | **Usage**: 4 | **Access level**: account
- **Source**: manual | **Tags**: frontend, logging

### Circuit breaker pattern for external services

External calls use circuit breaker (Ai::CircuitBreaker model). Tracks failures, opens when threshold exceeded, prevents cascading failures.

- **Quality**: 0.57 | **Usage**: 3 | **Access level**: account
- **Source**: manual | **Tags**: resilience, pattern, circuit-breaker

### Actions in PageContainer only

Frontend page actions (buttons, links) go in PageContainer's actions prop. Never add action buttons in page content area.

- **Quality**: 0.56 | **Usage**: 3 | **Access level**: account
- **Source**: manual | **Tags**: frontend, layout

### No any types in TypeScript

TypeScript code must use proper types — never `any`. Define interfaces and types for all data structures.

- **Quality**: 0.56 | **Usage**: 3 | **Access level**: account
- **Source**: manual | **Tags**: frontend, typescript

### Api V1 namespace for all controllers

All API controllers: Api::V1 namespace, inherit ApplicationController. Route prefix: /api/v1/. Enables API versioning.

- **Quality**: 0.56 | **Usage**: 5 | **Access level**: account
- **Source**: manual | **Tags**: api, controller, namespace

### Consistent error response format

Errors via render_error with message and status. Format: { success: false, error: { message: '...', code: '...' } }.

- **Quality**: 0.51 | **Usage**: 4 | **Access level**: account
- **Source**: manual | **Tags**: api, errors, response

### Named routes before id parameter routes

Place named routes (string segments) before /:id in routes.rb. Otherwise :id matches the named route's string.

- **Quality**: 0.51 | **Usage**: 5 | **Access level**: account
- **Source**: manual | **Tags**: routes, rails

### Never separate indexes for t.references

t.references auto-creates an index. Never add_index separately. Customize inline: t.references :account, index: { unique: true }.

- **Quality**: 0.50 | **Usage**: 2 | **Access level**: account
- **Source**: manual | **Tags**: migration, database, index

## Fact (28)

### Worker HTTP API only architecture

Worker Architecture (worker/): Standalone Sidekiq 7.2 process, communicates with server via HTTP API only. Never create jobs in server/app/jobs/.

## Stats
- 175 job files across 16 namespaces: ai_workflow, analytics, compliance, devops, docker, file_processing, git, integrations, maintenance, marketing, mcp, notifications, reports, services, swarm, webhooks + 85 flat (top-level) jobs
- 41 worker services + 4 API clients (BackendApiClient, ApiClient, WebAuthApiClient, LlmProxyClient)
- 32 weight...

- **Quality**: 0.90 | **Usage**: 14 | **Access level**: account
- **Source**: manual | **Tags**: architecture, worker, sidekiq

### AI Feature File Paths: Models, Services, and Frontend Locations

AI features are organized across three directories:
- Models: server/app/models/ai/ — all AI domain models (agents, teams, workflows, memory, tools, knowledge)
- Services: server/app/services/ai/ — all AI service classes (orchestration, memory, tools, providers, knowledge)
- Frontend: frontend/src/features/ai/ — React components, hooks, and pages for the AI feature module

These paths follow the namespace convention where AI models use the Ai:: Ruby namespace (e.g., Ai::Agent, Ai::AgentTeam, Ai:...

- **Quality**: 0.74 | **Usage**: 9 | **Access level**: team
- **Source**: agent | **Tags**: memory-bridge, ai, file-paths, project-structure

### Git Conventions and Backend Code Hygiene Rules

Critical git and code conventions that must be followed:

Git Rules:
- Tag naming: NO "v" prefix — use 0.2.0, not v0.2.0. Release branches: release/0.2.0
- No Claude attribution: Never include "Co-Authored-By" or "Generated with" in commit messages
- Submodule push order: ALWAYS push enterprise submodule before the parent repo to avoid dangling references

Backend Code Hygiene:
- Every Ruby file must start with: # frozen_string_literal: true
- Logging: Rails.logger only — never puts, print, or p...

- **Quality**: 0.74 | **Usage**: 11 | **Access level**: team
- **Source**: agent | **Tags**: memory-bridge, git, conventions, backend, code-hygiene

### pgvector Models and SharedKnowledge Provenance Column Gotcha

Models that use pgvector embeddings via the neighbor gem (has_neighbors :embedding):
- CompoundLearning — compound learning entries with decay and reinforcement
- DocumentChunk — RAG document chunks for knowledge base queries
- ContextEntry — context window entries for agent conversations
- RagQuery — cached RAG query results
- SharedKnowledge — shared knowledge entries (note: uses provenance column, NOT metadata)

The SharedKnowledge provenance gotcha: Unlike other models that store extra info...

- **Quality**: 0.72 | **Usage**: 5 | **Access level**: team
- **Source**: agent | **Tags**: memory-bridge, pgvector, models, embeddings, gotchas

### JWT Authentication Gotchas: Sub Claim, Dual Auth Systems, Proxy Masking, Token Size

Critical authentication pitfalls that cause hard-to-debug failures:

1. Sub Claim Required: handle_user_token does User.find(payload[:sub]) — using user_id or any other claim name causes silent auth failure. The JWT payload MUST have: sub (user UUID), account_id, email, type: "access", version: 2.

2. Two Authentication Systems: The platform runs dual auth:
   - JWT (main API): Authorization Bearer token for all /api/v1/* endpoints
   - Doorkeeper OAuth 2.1 (MCP endpoint): Used for AI agent and...

- **Quality**: 0.71 | **Usage**: 9 | **Access level**: team
- **Source**: agent | **Tags**: memory-bridge, authentication, jwt, gotchas, security, oauth

### Systemd Service Gotchas: RVM, StartLimit, Ruby Version, ProtectSystem, Redis DB

Systemd service configuration pitfalls for the Powernode platform:

1. RVM + set -u Conflict: Never use "set -u" (nounset) in scripts that source RVM. RVM uses unset variables internally. Use "set -eo pipefail" instead.

2. StartLimitIntervalSec/StartLimitBurst Location: These directives belong in the [Unit] section, NOT [Service]. Placing them in [Service] silently ignores them and services restart indefinitely on failure.

3. POWERNODE_RUBY_VERSION not RUBY_VERSION: Always use POWERNODE_RUBY_V...

- **Quality**: 0.70 | **Usage**: 6 | **Access level**: team
- **Source**: agent | **Tags**: memory-bridge, systemd, infrastructure, gotchas, rvm, redis

### RVM and systemd never use set -u

Never set -u (nounset) when sourcing RVM in systemd — uses uninitialized vars. Use set -eo pipefail only. Also use POWERNODE_RUBY_VERSION (not RUBY_VERSION).

- **Quality**: 0.64 | **Usage**: 8 | **Access level**: account
- **Source**: manual | **Tags**: systemd, rvm, gotcha

### PostgreSQL with pgvector extension

PostgreSQL with pgvector for vector similarity search. neighbor gem (0.6.0) provides Rails integration. HNSW indexes (not IVFFlat) — works on empty tables.

- **Quality**: 0.62 | **Usage**: 11 | **Access level**: account
- **Source**: manual | **Tags**: database, pgvector, ai

### Service ports for platform components

Default ports: backend=3000, frontend=3001, worker-web=4567. Configured in systemd environment files at /etc/powernode/.

- **Quality**: 0.62 | **Usage**: 12 | **Access level**: account
- **Source**: manual | **Tags**: infrastructure, ports, services

### StartLimitIntervalSec belongs in Unit section

Systemd: StartLimitIntervalSec and StartLimitBurst belong in [Unit], not [Service]. Placing in [Service] silently ignores them.

- **Quality**: 0.61 | **Usage**: 4 | **Access level**: account
- **Source**: manual | **Tags**: systemd, gotcha, infrastructure

### UUIDv7 primary keys on all tables

All database tables use UUIDv7 primary keys. Time-ordered, globally unique, index-friendly identifiers supporting distributed systems.

- **Quality**: 0.61 | **Usage**: 7 | **Access level**: account
- **Source**: manual | **Tags**: database, architecture

### Redis database separation

Worker uses Redis DB 1 (redis://localhost:6379/1). Server uses Redis DB 0 for caching/sessions. Prevents key collisions.

- **Quality**: 0.60 | **Usage**: 6 | **Access level**: account
- **Source**: manual | **Tags**: infrastructure, redis

### find_default_agent picks arbitrary order

find_default_agent picks first active agent by arbitrary DB order. May select Ollama over Grok. Set default_agent explicitly on RalphLoop.

- **Quality**: 0.60 | **Usage**: 8 | **Access level**: account
- **Source**: manual | **Tags**: ai, gotcha, agent

### Embedding service with Redis caching

Ai::Memory::EmbeddingService: vector embeddings with Redis cache (7-day TTL). OpenAI text-embedding-3-small (1536d) or Ollama. Key: ai:embeddings:{account}:{hash}.

- **Quality**: 0.60 | **Usage**: 9 | **Access level**: account
- **Source**: manual | **Tags**: ai, embeddings, caching

### JWT authentication system

JWT tokens with configurable expiry. Issued on login, validated per request. Frontend stores tokens in Authorization headers.

- **Quality**: 0.60 | **Usage**: 14 | **Access level**: account
- **Source**: manual | **Tags**: security, authentication

### ProtectSystem strict blocks home directory

ProtectSystem=strict makes filesystem read-only. Don't use in dev mode — blocks home directory and project files.

- **Quality**: 0.60 | **Usage**: 8 | **Access level**: account
- **Source**: manual | **Tags**: systemd, gotcha, security

### Rails 8 API only mode

Backend is Rails 8 API-only. No views, no asset pipeline. All responses are JSON via render_success/render_error.

- **Quality**: 0.60 | **Usage**: 9 | **Access level**: account
- **Source**: manual | **Tags**: architecture, rails

### Enterprise submodule pattern

Enterprise features at extensions/enterprise/ (git submodule). When absent, app runs single-user self-hosted, all features unlocked. Gate: Shared::FeatureGateService.enterprise_loaded? (backend), __ENTERPRISE__ (frontend).

- **Quality**: 0.59 | **Usage**: 8 | **Access level**: account
- **Source**: manual | **Tags**: architecture, enterprise

### Worker Scheduling: 27 Cron Entries via sidekiq-scheduler

27 cron entries in worker/config/sidekiq.yml :schedule: section. By frequency: Every 1 min (swarm_cluster_sync, docker_host_sync). Every 5 min (swarm_health_check, git_runner_health_check, docker_health_check). Every 10 min (ai_provider_health_check). Hourly (devops_approval_expiry, ai_workflow_approval_expiry, ai_budget_rollover). Every 6h (account_termination_processing, ai_provider_model_sync, chat_session_cleanup). Daily 1-5:30 AM UTC (ai_pricing_sync, daily_database_backup, data_retention,...

- **Quality**: 0.58 | **Usage**: 0 | **Access level**: team
- **Source**: agent | **Tags**: none

### Worker Queue Architecture: 32 Weighted Queues in 3 Tiers

32 weighted queues in worker/config/sidekiq.yml. Tier 1 weight-3 (critical): critical, high, workflow_high_priority, subscription_lifecycle, ai_cancellations, devops_high. Tier 2 weight-2 (standard): ai_workflows, ai_agents, ai_conversations, ai_execution, ai_orchestration, ai_workflow_health, ai_workflow_schedules, ai_workflow_nodes, ai_testing, devops_default, devops_webhooks, file_processing, services, billing, billing_scheduler, compliance, email, reports, integrations, mcp. Tier 3 weight-1...

- **Quality**: 0.58 | **Usage**: 0 | **Access level**: team
- **Source**: agent | **Tags**: none

### Worker Middleware and 13 Job Concerns

Middleware: SidekiqWebAuth (worker/app/middleware/) authenticates Sidekiq Web UI users via WebAuthApiClient against server. 13 job concerns in worker/app/jobs/concerns/: ai_jobs_concern (circuit breakers 600s AI, 120s backend), ai_llm_proxy_concern (LLM proxy client), ai_cost_calculation_concern, chat_streaming_concern (ActionCable), chat_fallback_providers_concern, a2a_artifact_extraction_concern, health_check_steps_concern, health_data_fetchers_concern, metrics_tracking, reports/csv_json_repor...

- **Quality**: 0.58 | **Usage**: 0 | **Access level**: team
- **Source**: agent | **Tags**: none

### No parallel test processes

Never run multiple rspec processes on same database. parallel_tests removed due to TRUNCATE deadlocks. Single process only.

- **Quality**: 0.58 | **Usage**: 6 | **Access level**: account
- **Source**: manual | **Tags**: testing, database, parallel

### Gitea slashed branch refs need resolve_ref

Gitea contents API fails for slashed refs (mission/feature-name). GiteaApiClient#resolve_ref converts to commit SHA via get_branch first.

- **Quality**: 0.58 | **Usage**: 4 | **Access level**: account
- **Source**: manual | **Tags**: gitea, gotcha, devops

### RalphTask has no name column

RalphTask: use task_key (identifier) and description (details). No name column. No key shortcut — always task_key.

- **Quality**: 0.56 | **Usage**: 2 | **Access level**: account
- **Source**: manual | **Tags**: ai, gotcha, ralph

### Feature flags via Flipper

Feature flags via Flipper: Flipper.enabled?(:feature_name) or Flipper.enabled?(:feature_name, actor). Notable: :compound_learning_injection, :compound_learning_promotion.

- **Quality**: 0.56 | **Usage**: 4 | **Access level**: account
- **Source**: manual | **Tags**: architecture, feature-flags

### Rate limiting on API endpoints

API endpoints have rate limiting. Stricter on auth endpoints, relaxed on data retrieval. Configured per-endpoint.

- **Quality**: 0.56 | **Usage**: 7 | **Access level**: account
- **Source**: manual | **Tags**: api, security, rate-limiting

### SharedKnowledge uses provenance not metadata

Ai::SharedKnowledge uses `provenance` column (not `metadata`). Always entry.provenance, not entry.metadata.

- **Quality**: 0.56 | **Usage**: 3 | **Access level**: account
- **Source**: manual | **Tags**: ai, gotcha, model

### compare_commits empty for slashed branches

Gitea compare_commits API returns empty for slashed branch names. Use list_commits as workaround.

- **Quality**: 0.54 | **Usage**: 4 | **Access level**: account
- **Source**: manual | **Tags**: gitea, gotcha, devops

