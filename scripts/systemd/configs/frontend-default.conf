# Powernode Frontend Instance Configuration
# Instance: default
# Installed to /etc/powernode/frontend-default.conf

# Dev server binding
PORT=3001
HOST=0.0.0.0

# API connection
VITE_API_BASE_URL=http://localhost:3000
VITE_WS_BASE_URL=ws://localhost:3000

# Build mode
NODE_ENV=development
